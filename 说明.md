# Frida 17.17.0 去特征版 · 使用说明

> 对**原版 frida 17.17.0** 的 frida-server / frida-gadget 做了源码级去特征，
> **客户端保持原版**——PC 端装官方 `pip install frida==17.17.0` 即可连接，无需重编客户端。
>
> ⚠️ 仅用于**授权的安全测试 / 红队演练**。

---

## 〇、版本要点

| 项目 | 说明 |
|------|------|
| 版本 | frida 17.17.0（源码级去特征，非二进制 patch） |
| 客户端 | **原版 pip frida 17.17.0**（协议串运行时值未变，兼容） |
| 架构 | android-arm64 |
| 默认端口 | `27042` → `49374`（可用 `-l` 再指定） |
| 构建方式 | GitHub Actions（NDK r29） |
| gadget 特性 | **minizip 已禁用**（APK 资源读取不可用；Zygisk/script 模式不受影响） |

---

## 一、产物清单

| 文件 | 用途 |
|------|------|
| `frida-server-17.17.0-defeatured` | 去特征版 frida-server（root 设备上用，ELF 可执行文件） |
| `frida-gadget-17.17.0-defeatured.so` | 去特征版 frida-gadget arm64（注入/嵌入目标进程的 `.so`） |
| `frida-gadget-17.17.0-defeatured-armeabi-v7a.so` | 去特征版 frida-gadget 32 位（armeabi-v7a，32 位 App 用） |
| `frida-inject-17.17.0-defeatured` | 注入器（`frida-inject -n <pid> <path>`） |
| `de-frida.py` | 去特征补丁脚本（幂等，重建源码用） |
| `.github/workflows/build.yml` | GitHub Actions 一键构建流程 |

---

## 二、frida-server 部署与使用

### 1. 部署到设备（需 root）

```bash
# PC → 设备
adb push frida-server-17.17.0-defeatured /data/local/tmp/fs

# 设备上（root）
adb shell
su
mv /data/local/tmp/fs /data/local/tmp/fs
chmod 755 /data/local/tmp/fs
/data/local/tmp/fs -l 0.0.0.0:8888 &
```

> - 默认端口已改 `49374`；`-l 0.0.0.0:8888` 可改用任意端口（推荐随机高位端口）。
> - 二进制名改 `fs`（或其他无特征名）即规避进程名检测；源码里 prgname 已是 `gsvc`。

### 2. PC 端连接（原版客户端）

```bash
# 装原版客户端（Windows/Linux 均可）
pip install frida==17.17.0 frida-tools

# USB 端口转发
adb forward tcp:8888 tcp:8888

# 附加（attach，最常用）
frida -H 127.0.0.1:8888 -n com.example.app -l hook.js

# 冷启动（spawn）
frida -H 127.0.0.1:8888 -f com.example.app -l hook.js
```

> **attach 与 spawn**（2026-08-18 OnePlus 8T 真机实测，均可用）：
> - attach：附加到已运行进程，**已实测可用**（arm64 进程注入，脚本执行正常）。
> - spawn：冷启动注入，需要 helper；**已实测可用**（`com.coloros.alarmclock` 冷启动注入成功）。
>   注：早期构建 helper 类名替换不全导致 server 启动即崩、spawn 不可用，已修复。

---

## 三、frida-gadget 部署与使用

gadget 是 `.so`，**直接加载进目标进程**，不需要 ptrace attach → 可绕过"自 ptrace 反调试"型 App。

### 1. 配置（config.json，放在 gadget 同目录）

**推荐：script 模式**（不监听任何端口/socket，最隐身）

```json
{
  "interaction": {
    "type": "script",
    "path": "/data/local/tmp/gsvc-hook.js",
    "on_change": "ignore"
  }
}
```

- 加载即执行 hook 脚本，**无 TCP 端口、无 unix socket、无额外文件痕迹**。

**备选：listen 模式**（需要 PC 客户端连）

```json
{
  "interaction": {
    "type": "listen",
    "port": 49374,
    "address": "127.0.0.1",
    "on_load": "wait"
  }
}
```

```bash
adb forward tcp:49374 tcp:49374
frida -H 127.0.0.1:49374 -n <process> -l hook.js
```

### 2. 部署方式

**方案 A：Zygisk 模块注入（推荐，免重打包）**

Magisk Zygisk 模块，在 zygote fork 出目标 App 后、`nativeForkAndSpecialize` 返回前，
把 gadget `.so` `dlopen` 进目标进程（仅目标包名）。

- gadget `.so` 改名（如 `libsecurity.so`）放进模块 `zygisk/arm64-v8a/`。
- 用 script 模式配置，避免监听端口被扫。

**方案 B：APK 重打包（免 root）**

`apktool` 解包 → gadget `.so` 放进 `lib/arm64-v8a/` → 在 `Application.attachBaseContext`
或 `.init_array` 里 `System.loadLibrary(...)` 加载 → 重签名。

> ⚠️ 注意：gadget **加载进进程那一刻**，`/proc/self/maps` 会多出该 `.so` 的映射。
> **必须把 `.so` 改名为无特征名**（如 `libSecurityProvider.so`）。
>
> ⚠️ **minizip 已禁用**：gadget 无法再从 `xxx.apk!assets/...` 读取配置（冷门特性）。
> 配置请用独立 `config.json` 文件。

---

## 四、frida-inject

```bash
adb push frida-inject-17.17.0-defeatured /data/local/tmp/fi
# 设备上 root
/data/local/tmp/fi -n <pid> /data/local/tmp/hook.js
```

---

## 五、验证去特征是否生效

**设备侧：**

```bash
netstat -tuln | grep 27042      # 应无结果（端口已改 49374）
ps -A | grep frida              # 应无结果（二进制已改名 fs）
```

**目标 App 进程内线程名**应读到 `js-loop` / `evtloop` / `ipcbus` / `pool-gsvc`，而非
`gum-js-loop` / `gmain` / `gdbus` / `pool-frida`。

**二进制字符串扫描：**

```bash
strings frida-server-17.17.0-defeatured | grep -cE "gum-js-loop|27042"   # 0
strings frida-gadget-17.17.0-defeatured.so | grep -cE "gum-js-loop|27042" # 0
```

---

## 六、已去除 / 刻意保留的特征

**已去除（服务端/设备端进程特征）：**

| 特征 | 原值 | 改为 |
|------|------|------|
| 默认端口 | `27042` | `49374` |
| 进程名 prgname | `frida` | `gsvc` |
| 线程名 | `gum-js-loop` / `gmain` / `gdbus` | `js-loop` / `evtloop` / `ipcbus` |
| 线程名 | `frida-gadget*` / `frida-main-loop` / `frida-eternal-agent` 等 | `gsvc-*` |
| agent 注入资源名 | `frida-agent-arm64.so` 等（App maps 可见） | `gsvc-agent-*` |
| helper memfd/socket | `frida-helper-32/64`、`/frida-<uuid>` | `gsvc-*`、`/gsvc-*` |
| helper Java 包 + 类名 | `re.frida.Helper` / `re.frida.HelperBackend` | `re.gsvc.Helper` / `re.gsvc.HelperBackend`（含重新编译 dex） |
| helper 内部 D-Bus 名 | `re.frida.Helper` / `/re/frida/Helper` | `re.gsvc.Helper` / `/re/gsvc/Helper`（server↔helper 内部协议，客户端不引用） |
| gadget 显示名 | `Gadget` | `Gsvc` |
| RPC 协议串字面量 | `frida:rpc`（frida-core 侧） | base64 运行时解码（值不变） |

**刻意保留（客户端协议红线，值必须与 stock 一致）：**

- `re.frida.Gadget` / `re.frida.Server` / `re.frida.HostSession*` 等**客户端可见** D-Bus 名 —— 客户端据此通信
  （helper 内部 D-Bus 名 `re.frida.Helper` 已改 `re.gsvc.Helper`，客户端不感知）
- `lolcathost`（TLS origin/SNI）—— 客户端握手用
- `frida:rpc`（agent JS 派发器，frida-gum 运行时）—— 与 strongR 同款处理
- `frida:stdout` / `frida:stderr`（switch case 无法运行时解码）
- `Frida/` UA/Server 头、`Frida` JS 全局名
- `libfrida-gadget-raw.so`（meson 产物名/SONAME，运行时改名即可）

> 若目标 App 做**全内存扫 "frida" 子串**，上述刻意保留项可能被命中；
> 只做**特征点检测**（maps/线程名/端口/`frida:rpc` 服务端侧）的 App 检测不到。

---

## 七、常见问题

**Q1：为什么官方客户端能连？不是改了 D-Bus 吗？**
本版**没有改 `re.frida.*` 总线名**（改了客户端就连不上，需要成套重编）。
只把 `frida:rpc` 字面量做了**运行时保值混淆**——二进制里没有，运行时值不变。

**Q2：spawn（-f）失败？**
本版已修复（2026-08-18 真机验证）：helper 类名全套替换后 server 不再启动即崩，
spawn 在 OnePlus 8T / ColorOS 上实测可用。若用的是官方原版 frida 仍失败，属设备环境兼容问题，改用 attach。

**Q3：gadget 不能读 APK 里的配置了？**
对，minizip 已禁用（绕开交叉编译依赖问题）。用独立 `config.json`。

**Q4：能完全绕过检测吗？**
静态特征（端口/线程名/进程名/agent 名）已去除。但 **ptrace 占坑、Inline Hook/CRC 完整性校验**
属于运行时反调试，仍需在 hook 脚本里处理。

---

## 八、重新构建

源码在 GitHub 仓库 `c9mt4rpzsj-droid/frida-defeatured`，`.github/workflows/build.yml`
双 job 并行构建 server + gadget：

1. **Actions → build-defeatured-frida-17 → Run workflow**
2. 完成后在 **Summary → Artifacts** 下载两个产物包

> 关键构建参数（踩坑记录）：
> - **NDK 必须 r29**（frida 17.17.0 硬性要求，r25b 的 libc++ fstream 补丁会失败）
> - gadget 需 **`--without-prebuilds=sdk:host`**（glib 从源码构建以打线程名补丁）
> - gadget 的 minizip 依赖在 android 交叉编译有 zlib-ng/fseeko 问题 → **已禁用**（见 de-frida.py 与 workflow）
> - **helper 类名必须全套替换**：`re.frida.Helper` / `re/frida/Helper` / `package re.frida;` 全部要换成 gsvc 变体。
>   漏任何一个（旧脚本只匹配带尾随空格的 `"re.frida.Helper "`，实际匹配不到任何源码），
>   vala 侧 `find_class("re/frida/HelperBackend")` 就找不到已重编译为 `re/gsvc.*` 的类
>   → `backend_class == null` → server **启动即断言崩溃** Bail out。（见 de-frida.py helper 段注释）
